# IBM i source workspace

Open only this folder in Bob IDE.

Contents Bob is allowed to see:

- `qrpglesrc/`, `qrpgleref/`, `qddssrc/`, `qclsrc/`, `qsqlsrc/` — a small
  connected IBM i application (RPGLE, copybook, DDS, CLLE, SQL)
- `ticket-EXP-01.pdf` — the change request to analyze
